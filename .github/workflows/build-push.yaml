name: Build and Push Docker Images
on:
  push:
    branches:
      - main
    paths:
      - 'api/**'
      - 'ui/**'
      - 'controller/**'
      - 'worker/**'
      - '.github/workflows/build-push.yaml'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    strategy:
      matrix:
        component: [api, ui, controller, worker]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set image tag
        id: tag
        run: echo "tag=sha-$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          IMAGE=$ECR_REGISTRY/shipzen/${{ matrix.component }}
          TAG=$(git rev-parse --short HEAD)
          docker build \
            --build-arg NEXT_PUBLIC_API_URL=https://shipzen.jeneeldumasia.codes/api/v1 \
            -t $IMAGE:sha-$TAG \
            -t $IMAGE:latest \
            ./${{ matrix.component }}
          docker push $IMAGE:sha-$TAG
          docker push $IMAGE:latest

  update-kustomization:
    # Single job — runs after ALL matrix builds succeed.
    # Updating kustomization.yaml in one atomic commit avoids the race condition
    # where parallel matrix jobs clobber each other's tag updates.
    needs: build-and-push
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Fetch the very latest commit so we rebase on top of it
          ref: main
          fetch-depth: 0

      - name: Update all component tags in kustomization.yaml
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          TAG="sha-${SHORT_SHA}"
          echo "Updating all built components to tag: $TAG"

          python3 - <<'PYEOF'
          import re, os

          tag = os.environ["TAG"]
          components = ["api", "ui", "controller", "worker"]
          path = "infra/kustomization.yaml"

          with open(path, "r") as f:
              content = f.read()

          # Update each component's newTag using a pattern that matches the
          # name: / newName: / newTag: block regardless of existing blank lines
          for c in components:
              content = re.sub(
                  rf'(name: shipzen-{c}\n\s+newName:[^\n]+\n\s+newTag:) [^\n]+',
                  rf'\1 {tag}',
                  content
              )

          # Collapse any run of >2 consecutive blank lines to 1 blank line
          # to prevent the file from accumulating blank lines on repeated runs
          content = re.sub(r'\n{3,}', '\n\n', content)

          with open(path, "w") as f:
              f.write(content)
          PYEOF
        env:
          TAG: sha-$(git rev-parse --short HEAD)

      - name: Commit and push tag update
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          TAG="sha-${SHORT_SHA}"

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add infra/kustomization.yaml

          if git diff-index --quiet HEAD; then
            echo "No tag changes to commit."
            exit 0
          fi

          git commit -m "chore: update platform images to ${TAG} [skip ci]"

          # Retry loop handles any last-second push from another workflow
          for i in {1..5}; do
            git pull --rebase origin main
            if git push origin main; then
              echo "Successfully pushed kustomization update"
              exit 0
            fi
            echo "Push attempt $i failed, retrying in 5s..."
            sleep 5
          done
          echo "ERROR: failed to push after 5 attempts"
          exit 1
