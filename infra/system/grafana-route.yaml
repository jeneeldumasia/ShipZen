apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-route
  namespace: observability
spec:
  parentRefs:
    - name: deployhub-gateway
      namespace: deployhub-system
  hostnames:
    - "grafana.deployhub.jeneeldumasia.codes"
  rules:
    - backendRefs:
        - name: kube-prometheus-stack-grafana
          port: 80
