apiVersion: v1
kind: Service
metadata:
  name: deployhub-ui
  namespace: deployhub-system
  labels:
    app: deployhub-ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: deployhub-ui
