apiVersion: v1
kind: Service
metadata:
  name: deployhub-api
  namespace: deployhub-system
  labels:
    app: deployhub-api
spec:
  type: ClusterIP
  selector:
    app: deployhub-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
