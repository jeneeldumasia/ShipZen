apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: deployhub-api
  namespace: deployhub-system
spec:
  parentRefs:
    - name: deployhub-gateway
      namespace: deployhub-system
      sectionName: https
  hostnames:
    - "api.deployhub.jeneeldumasia.codes"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: deployhub-api
          port: 80
